// tsconfig.json
{
  "compilerOptions": {
    // Ignora checagem de tipos em arquivos de bibliotecas (resolve erros falsos)
    "skipLibCheck": true,

    // (Opcional) Se você criou augmentations em src/types:
    "typeRoots": ["node_modules/@types", "src/types"]
  },
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
